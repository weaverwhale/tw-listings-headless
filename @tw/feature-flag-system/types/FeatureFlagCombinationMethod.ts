export enum FeatureFlagCombinationMethod {
  OR = 'OR',
  AND = 'AND',
  MIN = 'MIN',
  MAX = 'MAX',
  SUM = 'SUM',
  ARRAY_CONCAT = 'ARRAY_CONCAT',
  ARRAY_INTERSECT = 'ARRAY_INTERSECT',
  ARRAY_COMBINE_BY_RANK = 'ARRAY_COMBINE_BY_RANK',
}
